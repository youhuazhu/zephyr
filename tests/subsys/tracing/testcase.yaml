tests:
  tracing.transport.uart.ctf:
    extra_args: CONF_FILE="prj_uart_ctf.conf"
    platform_allow: qemu_x86 qemu_x86_64
    harness: pytest
    harness_config:
      handle_script: tests/subsys/tracing/test_ctf.py
    tags: tracing_uart_ctf
